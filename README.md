# Планировщик задач (TODO-сервер)

Веб-сервер для управления задачами на Go с базой данных SQLite. Реализует REST API для добавления, получения, редактирования, удаления и переноса повторяющихся задач.

## Задания со звёздочкой

- Реализован **базовый функционал** (правила повторения `d` и `y`).
- Задания со звёздочкой **не выполнялись**.
